# make.configuration.defn file for thorn AMReX

# Define standard AMReX utilities and install those that we could build/that are
# present in the system
STD_AMREX_UTILS = 
ALL_UTILS += $(STD_AMREX_UTILS)
